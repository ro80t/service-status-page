# Used by drizzle-kit (db:generate / db:migrate) to reach Postgres directly.
# In production this is the same Neon database that Hyperdrive proxies.
DATABASE_URL="postgres://user:0000@localhost:1234/test"
